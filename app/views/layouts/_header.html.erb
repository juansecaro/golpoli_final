<nav class="navbar navbar-default navbar-fixed-top ">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div id="logo">
        <%= link_to image_tag("poligol2.png", alt: "Poligol logo"),root_path %>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= "active" if current_page?("/") %>"><%= link_to "Inicio", root_path %></li>

        <% unless user_signed_in? %>
          <li class="<%= "active" if current_page?("/users/sign_up") %>"> <%= link_to "Regístrate", new_user_registration_path %> </li>
          <li class="<%= "active" if current_page?("/users/sign_in") %>"> <%= link_to "Acceder", new_user_session_path %> </li>
        <%  end %>

      </ul>


      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="<%= "active" if current_page?("/users/edit") %>"> <%= link_to edit_user_registration_path do %>
              <span>image</span> <%=current_user.email%>
            <% end %>
          </li>
          <li class="<%= "active" if current_page?("/users/#{current_user.email}") %>"> <%= link_to "My events", user_path(current_user) %> </li>
          <li><%= link_to "Salir", destroy_user_session_path, method: :delete %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
